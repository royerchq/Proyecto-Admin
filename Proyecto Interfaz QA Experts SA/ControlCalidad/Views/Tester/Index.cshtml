@model IEnumerable<ControlCalidad.Models.Tester>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cantidadReqAsignados)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Empleado.nombreP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cantidadReqAsignados)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Empleado.nombreP)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.cedula_empleadoFk }) |
            @Html.ActionLink("Details", "Details", new { id=item.cedula_empleadoFk }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cedula_empleadoFk })
        </td>
    </tr>
}

</table>
